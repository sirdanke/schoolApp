<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <script src="main.js"></script>
    <% include ./partial/bootstrap %>
</head>
<body>
    <% include ./partial/navbar %>
    <h2>ADD <%= name.slice(0, name.length-1).toUpperCase() %> DATA</h2>
    <form action="/<%= name %>/add" method="POST">

        <% if(name == 'subjects') { %>
        <% include ./partial/data_subject %>
        <% } else { %>
        <% include ./partial/data_person %>
        <% } %>
    
        <button type="submit" class="btn btn-default">Add <%= name.slice(0, name.length-1) %></button>
    </form>
</div>
              
    
</body>
</html>